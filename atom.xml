<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.85.0">Hugo</generator><title type="html">reproducible build environments on ilo</title><link href="https://ilo.projects.metio.wtf/" rel="alternate" type="text/html" title="HTML"/><link href="https://ilo.projects.metio.wtf/index.xml" rel="alternate" type="application/rss+xml" title="RSS"/><link href="https://ilo.projects.metio.wtf/atom.xml" rel="self" type="application/atom+xml" title="Atom"/><link href="https://ilo.projects.metio.wtf/humans.txt" rel="alternate" type="text/plain" title="Humans"/><link href="https://ilo.projects.metio.wtf/foaf.rdf" rel="alternate" type="application/rdf+xml" title="FOAF"/><updated>2021-07-12T04:56:11+00:00</updated><id>https://ilo.projects.metio.wtf/</id><entry><title type="html">Alternatives</title><link href="https://ilo.projects.metio.wtf/community/alternatives/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/community/alternatives/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>It&rsquo;s highly likely that <code>ilo</code> does not provide the functionality you are looking for. Here is a list of alternatives that might cover your needs:</p>
<ul>
<li><a href="https://github.com/containers/toolbox">toolbox</a>: Toolbox is a tool that offers a familiar package based environment for developing and debugging software that runs fully unprivileged using Podman.</li>
<li><a href="https://gitian.org/">gitian</a>: Gitian is a secure source-control oriented software distribution method.</li>
<li><a href="https://rbm.torproject.org/">rbm</a>: Reproducible Build Manager (rbm) is a tool that helps you create and build packages for multiple linux distributions, and automate the parts that can be automated.</li>
<li><a href="https://github.com/nerdvegas/rez">rez</a>: Rez is a cross-platform package manager with a difference.</li>
<li><a href="https://github.com/mottosso/bleeding-rez">bleeding-rez</a>: Rez is a command-line utility for Windows, Linux and MacOS, solving the problem of creating a reproducible environment for your software projects on any machine in any pre-existing environment.</li>
<li><a href="http://rbld.io/">rebuild</a>: A strategic approach to managing build and test environments for IoT and embedded development.</li>
<li><a href="https://github.com/jasonwhite/ducible">ducible</a>: This is a tool to make builds of Portable Executables (PEs) and PDBs reproducible.</li>
<li><a href="https://buildpacks.io/">buildpacks</a>: Buildpacks are pluggable, modular tools that translate source code into OCI images.</li>
<li><a href="https://nixos.org/nix/">nix</a>: Nix makes it trivial to set up and share build environments for your projects, regardless of what programming languages and tools you’re using.</li>
<li><a href="https://github.com/rosineygp/mkdkr">mkdkr</a>: Super small and powerful framework for build CI pipeline, scripted with Makefile and isolated with docker.</li>
<li><a href="https://virtualenv.pypa.io/en/stable/">virtualenv</a>: virtualenv is a tool to create isolated Python environments.</li>
<li><a href="https://github.com/Zlika/moot">moot</a>: A shell script to easily select, download and run the versions of Maven and JDK you want for your build.</li>
<li><a href="https://paketo.io/">paketo</a>: Modular Buildpacks, written in Go. Paketo Buildpacks provide language runtime support for applications. They leverage the Cloud Native Buildpacks framework to make image builds easy, performant, and secure.</li>
<li><a href="https://github.com/earthly/earthly">earthly</a>: run all your builds containerized</li>
<li><a href="https://www.gitpod.io/">gitpod</a>: Describe your dev environment as code and get fully prebuilt, ready-to-code development environments for any GitLab, GitHub, and Bitbucket project.</li>
<li><a href="https://www.vagrantup.com/">vagrant</a>: Vagrant is a tool for building and managing virtual machine environments in a single workflow.</li>
<li><a href="https://subuser.org/">subuser</a>: Subuser turns Docker containers into normal linux programs</li>
<li><a href="https://bobbuildtool.dev/">bob</a>: Bob is a functional build automation tool with an emphasis on cross compilation. It is intended for complex embedded projects and thus focuses on reproducible builds while still being nice to developers in agile environments.</li>
<li><a href="https://github.com/kudulab/dojo">dojo</a>: Containerize your development and operations environment</li>
<li><a href="https://github.com/kamilchm/go2nix">go2nix</a>: Reproducible builds and development environment for Go</li>
<li><a href="https://github.com/nixcloud/dep2nix">dep2nix</a>: dep2nix converts a Gopkgs.lock file into a deps.nix file which is understood by nixpkgs&rsquo;s go abstraction thanks to go2nix effort.</li>
<li><a href="https://github.com/nix-community/vgo2nix">vgo2nix</a>: Convert go.mod files to nixpkgs buildGoPackage compatible deps.nix files</li>
<li><a href="https://mhewedy.github.io/vermin/">vermin</a>: Vermin is a smart, simple and powerful command line tool for Linux, Windows and macOS. It&rsquo;s designed for developers who want a fresh VM environment with a single command.</li>
<li><a href="https://sdkman.io/">sdkman</a>: SDKMAN! is a tool for managing parallel versions of multiple Software Development Kits on most Unix based systems.</li>
<li><a href="https://github.com/shyiko/jabba">jabba</a>: Java Version Manager inspired by nvm (Node.js). Written in Go.</li>
<li><a href="https://github.com/nvm-sh/nvm">nvm</a>: nvm is a version manager for node.js, designed to be installed per-user, and invoked per-shell. nvm works on any POSIX-compliant shell (sh, dash, ksh, zsh, bash), in particular on these platforms: unix, macOS, and windows WSL.</li>
<li><a href="https://devspace.sh/">devspace</a>: DevSpace is an open-source developer tool for Kubernetes that lets you develop and deploy cloud-native software faster.</li>
<li><a href="https://github.com/rbenv/rbenv">rbenv</a>: Use rbenv to pick a Ruby version for your application and guarantee that your development environment matches production.</li>
<li><a href="https://rvm.io/">rvm</a>: RVM is a command-line tool which allows you to easily install, manage, and work with multiple ruby environments from interpreters to sets of gems.</li>
<li><a href="https://github.com/LoopPerfect/buckaroo">buckaroo</a>: The decentralized package manager for C++ and friends.</li>
<li><a href="https://github.com/polydawn/repeatr">repeatr</a>: Repeatr is a tool for running processes repeatedly. Repeatr is designed to make task definition precise, environment setup portable, and results reproducible.</li>
<li><a href="https://github.com/lando/lando">lando</a>: The local development and DevOps tool trusted by professional developers across the galaxy.</li>
<li><a href="https://github.com/drud/ddev">ddev</a>: ddev is an open source tool that makes it simple to get local PHP development environments up and running in minutes.</li>
<li><a href="https://gitlab.com/uppercat/tlbx">tlbx</a>: tlbx is a tool that facilitates creating and using mutable, persistent Linux container environments.</li>
<li><a href="https://github.com/DavHau/mach-nix">mach-nix</a>: Mach-nix makes it easy to create and share reproducible python environments.</li>
<li><a href="https://rstudio.github.io/renv/">renv</a>: The renv package helps you create reproducible environments for your R projects.</li>
<li><a href="https://github.com/getpopper/popper">popper</a>: Popper is a tool for defining and executing container-native workflows in Docker, as well as other container engines.</li>
<li><a href="https://github.com/dbhi/qus">qus</a>: qemu-user-static (qus) is a compilation of utilities, examples and references to build and execute OCI images (aka docker images) for foreign architectures using QEMU&rsquo;s user-mode emulation.</li>
<li><a href="https://github.com/cgwalters/coretoolbox">coretoolbox</a>: This is a new implementation of <a href="https://github.com/debarshiray/toolbox/">https://github.com/debarshiray/toolbox/</a></li>
<li><a href="https://github.com/garmin/pyrex">pyrex</a>: Containerize your bitbake.</li>
<li><a href="https://github.com/lxdock/lxdock">lxdock</a>: LXDock is a wrapper around LXD that allows developers to orchestrate their development environments using a workflow similar to Vagrant.</li>
<li><a href="https://github.com/bketelsen/devlx">devlx</a>: devlx is a development tool that provisions temporary development environments. It uses lxd and zfs to make efficient, copy-on-write, workspaces from a user-provided template.</li>
<li><a href="https://github.com/lueschem/edi">edi</a>: Driven by the DevOps mindset edi helps you to streamline your embedded development infrastructure.</li>
<li><a href="https://github.com/YunoHost/ynh-dev">ynh-dev</a>: ynh-dev is a CLI tool to manage your local development environment for YunoHost.</li>
<li><a href="https://github.com/Varying-Vagrant-Vagrants/VVV">VVV</a>: VVV is a local developer environment, mainly aimed at WordPress developers. It uses Vagrant and VirtualBox, and can be used to build sites, and contribute to WordPress.</li>
<li><a href="https://github.com/ianmiell/shutit">shutit</a>: Automation framework for programmers.</li>
<li><a href="https://github.com/vccw-team/vccw">vccw</a>: A Vagrant based development environment.</li>
<li><a href="https://github.com/mechboxes/mech">mech</a>: Easy command line virtual machines for VMWare.</li>
<li><a href="https://github.com/teracyhq/dev">dev</a>: The only truly universal productive software development platform for all!</li>
<li><a href="https://github.com/Chassis/Desktop">Desktop</a>: Local WordPress development made easy.</li>
<li><a href="https://github.com/stakkr-org/stakkr">stakkr</a>: Stakkr is a docker recompose tool that uses docker compose to easily create / maintain a stack of services, for example for web development.</li>
<li><a href="https://github.com/ulikoehler/buildock">buildock</a>: Reproducible build environments for local builds using Docker.</li>
<li><a href="https://github.com/tightenco/takeout">takeout</a>: Takeout is a Mac-based CLI tool for spinning up tiny Docker containers, one for each of your development environment dependencies.</li>
<li><a href="https://laravel.com/docs/7.x/valet">valet</a>: Valet is a Laravel development environment for Mac minimalists.</li>
<li><a href="https://python-poetry.org/">poetry</a>: Poetry comes with all the tools you might need to manage your projects in a deterministic way.</li>
<li><a href="https://github.com/nix-community/poetry2nix">poetry2nix</a>: poetry2nix turns Poetry projects into Nix derivations without the need to actually write Nix expressions.</li>
<li><a href="https://www.buildstream.build/">buildstream</a>: BuildStream is a powerful and flexible software integration toolset.</li>
<li><a href="https://www.sciviews.org/software/svbox/">svbox</a>: A Reproducible Software Environment for Data Science</li>
<li><a href="https://github.com/ctuning/ck">ck</a>: Collective Knowledge framework (CK) helps to organize any software project as a database of reusable components with common automation actions and extensible meta descriptions based on FAIR principles.</li>
<li><a href="https://github.com/BrainTwister/docker-devel-env">docker-devel-env</a>: Fast, reproducible, and portable software development environments</li>
<li><a href="https://github.com/luispedro/nixml">nixml</a>: Simple, perfectly reproducible, environments with nix specified using an YAML file.</li>
<li><a href="https://docs.haskellstack.org/en/stable/README/">Stack</a>: Stack is a cross-platform program for developing Haskell projects. It is aimed at Haskellers both new and experienced.</li>
<li><a href="https://conda.io/">Conda</a>: Package, dependency and environment management for any language—Python, R, Ruby, Lua, Scala, Java, JavaScript, C/ C++, FORTRAN, and more.</li>
<li><a href="https://github.com/smarr/rebench">rebench</a>: Execute and document benchmarks reproducibly.</li>
<li><a href="https://github.com/GreenCape/build">build</a>: A docker based test and build environment, especially for Joomla!</li>
<li><a href="https://github.com/target/lorri">lorri</a>: lorri is a nix-shell replacement for project development. lorri is based around fast direnv integration for robust CLI and editor integration.</li>
<li><a href="https://gitlab.com/njam/toolbox-njam">toolbox-njam</a>: Experimental Rust-clone of Fedora&rsquo;s toolbox for managing Linux containers for software development.</li>
<li><a href="https://github.com/nhoffman/bioscons">bioscons</a>: Extends the scons build tool for reproducible workflows in bioinformatics.</li>
<li><a href="https://github.com/rpm-software-management/mock">mock</a>: Mock is a tool for a reproducible build of RPM packages.</li>
<li><a href="https://github.com/jenv/jenv">jenv</a>: jEnv is a command line tool to help you forget how to set the JAVA_HOME environment variable</li>
<li><a href="https://asdf-vm.com/">asdf</a>: Manage multiple runtime versions with a single CLI tool</li>
</ul>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/community" term="community" label="Community"/><category scheme="https://ilo.projects.metio.wtf/tags/alternatives" term="alternatives" label="alternatives"/></entry><entry><title type="html">Argument Files</title><link href="https://ilo.projects.metio.wtf/usage/argument-files/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/usage/argument-files/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In order to share the same options/commands across your team, <code>ilo</code> supports argument files which contain the options for your project, e.g. which image you are using. Argument files are just plain text files and both name and location can be chosen at will. In order to use an argument file, you have to add <strong>@</strong> in front of the file name: <code>ilo @file-name</code>.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># write argument file</span>
$ cat some/folder/your-arguments.txt
shell
node:latest
/bin/bash

<span class="c1"># use argument file</span>
$ ilo @some/folder/your-arguments.txt
</code></pre></div><p>The argument file in the above example specified all commands and options on a new line, however you could write them all in a single line (or a mixture of both) as well:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># write argument file</span>
$ cat some/other/your-arguments.txt
shell node:latest /bin/bash

<span class="c1"># write argument file</span>
$ cat some/more/your-arguments.txt
shell
node:latest /bin/bash

<span class="c1"># use argument file</span>
$ ilo @some/other/your-arguments.txt
$ ilo @some/more/your-arguments.txt
</code></pre></div><p><strong>Important</strong>: In case your option contains a whitespace, you have to either put the entire option with its value in single/double quotes or use a whitespace between option and value like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># quote the entire option</span>
<span class="s2">&#34;--run-as=</span><span class="k">$(</span>id -u<span class="k">)</span><span class="s2">:</span><span class="k">$(</span>id -g<span class="k">)</span><span class="s2">&#34;</span>

<span class="c1"># quote the value</span>
--run-as <span class="s2">&#34;</span><span class="k">$(</span>id -u<span class="k">)</span><span class="s2">:</span><span class="k">$(</span>id -g<span class="k">)</span><span class="s2">&#34;</span>

<span class="c1"># THIS WON&#39;T WORK</span>
--run-as<span class="o">=</span><span class="s2">&#34;</span><span class="k">$(</span>id -u<span class="k">)</span><span class="s2">:</span><span class="k">$(</span>id -g<span class="k">)</span><span class="s2">&#34;</span>
</code></pre></div><p>You can use multiple arguments files which are evaluated in-order, e.g like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo @first @second
</code></pre></div><p>You can mix argument files with regular CLI options as well:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo shell @default-shell openjdk:11
</code></pre></div><p>The argument file used by <code>ilo</code> developers can be seen <a href="https://github.com/metio/ilo/blob/main/.ilo.rc">here</a> and is used by simply calling <code>ilo</code> in the root of the project because it&rsquo;s a <a href="../run-commands">run command</a> file.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/usage" term="usage" label="usage"/><category scheme="https://ilo.projects.metio.wtf/tags/argument-files" term="argument-files" label="argument files"/></entry><entry><title type="html">autoenv</title><link href="https://ilo.projects.metio.wtf/integration/autoenv/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/integration/autoenv/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In order to simplify the usage of <code>ilo</code>, consider using <a href="../../usage/run-commands">run commands</a>, <a href="../../usage/argument-files">argument files</a> and a <code>autoenv</code>-like tooling. These tools all allow you to just enter a directory and will automatically call <code>ilo</code> for you like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname ~<span class="o">]</span>$ <span class="nb">cd</span> path/to/your/project
<span class="o">[</span>root@container project-dir<span class="o">]</span><span class="c1">#</span>
</code></pre></div><p>As soon as you enter the directory of your project, these tools will call <code>ilo</code> which in turn will open your build environment for you.</p>
<h2 id="autoenv">autoenv</h2>
<p>In order to use <a href="https://github.com/inishchith/autoenv">autoenv</a>, first <a href="https://github.com/inishchith/autoenv#install">install it</a> and then place a <code>.env</code> file in the root of your project which contains <code>ilo</code> and its arguments as its contents.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat .env
ilo @build-env
</code></pre></div><h2 id="direnv">direnv</h2>
<p>In order to use <a href="https://direnv.net/">direnv</a>, first <a href="https://direnv.net/#basic-installation">install it</a> and then place a <code>.envrc</code> file in the root of your project which contains <code>ilo</code> and its arguments as its contents.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat .envrc
ilo @build-env
</code></pre></div><h2 id="envrc-rs">envrc-rs</h2>
<p>In order to use <a href="https://github.com/roxma/envrc-rs">envrc-rs</a>, first <a href="https://github.com/roxma/envrc-rs#install">install it</a> and then place a <code>.envrc</code> file in the root of your project which contains <code>ilo</code> and its arguments as its contents.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat .envrc
ilo @build-env
</code></pre></div><h2 id="smartcd">smartcd</h2>
<p>In order to use <a href="https://github.com/cxreg/smartcd">smartcd</a>, first <a href="https://github.com/cxreg/smartcd#ok-how-do-i-use-it">install it</a>, enter the root directory of your project and then call <code>smartcd edit enter</code> which will open an editor to write a script to execute whenever you enter your project directory. Use something like <code>ilo @build-env</code> as the script.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat ~/.smartcd/scripts/path/to/project-dir/bash_enter
ilo @build-env
</code></pre></div><h2 id="zsh-autoenv">zsh-autoenv</h2>
<p>In order to use <a href="https://github.com/Tarrasch/zsh-autoenv">zsh-autoenv</a>, first <a href="https://github.com/Tarrasch/zsh-autoenv#installation">install it</a> and then place a <code>.autoenv.zsh</code> file in the root of your project which contains <code>ilo</code> and its arguments as its contents.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat .autoenv.zsh
ilo @build-env
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/integration" term="integration" label="integration"/><category scheme="https://ilo.projects.metio.wtf/tags/autoenv" term="autoenv" label="autoenv"/><category scheme="https://ilo.projects.metio.wtf/tags/direnv" term="direnv" label="direnv"/><category scheme="https://ilo.projects.metio.wtf/tags/smartcd" term="smartcd" label="smartcd"/><category scheme="https://ilo.projects.metio.wtf/tags/zsh-autoenv" term="zsh-autoenv" label="zsh-autoenv"/></entry><entry><title type="html">Building ilo</title><link href="https://ilo.projects.metio.wtf/contributors/building/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/contributors/building/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p><code>ilo</code> requires a certain set of software installed on your system in order to be built.</p>
<h2 id="prerequisites">Prerequisites</h2>
<ul>
<li><a href="https://git-scm.com/">git</a> for version control</li>
<li><a href="https://jedisct1.github.io/minisign/">minisign</a> to sign the waiver as a contributor</li>
<li><a href="https://gnupg.org/">gpg</a> to sign your the commit which adds the waiver</li>
</ul>
<h2 id="manual-setup">Manual Setup</h2>
<ul>
<li><a href="https://jdk.java.net/">Java JDK</a> to compile the code</li>
<li><a href="https://maven.apache.org/">Maven</a> to build the project</li>
<li><a href="https://gohugo.io/">hugo</a> in order to create the website</li>
<li><a href="https://www.graalvm.org/">GraalVM</a> to build a native executable</li>
</ul>
<h2 id="ilo-setup">ilo Setup</h2>
<ul>
<li><a href="../../usage/install">ilo</a> to open the reproducible build environment for <code>ilo</code> itself</li>
<li>One of the <a href="../../shell/runtimes">runtimes</a> that <code>ilo shell</code> supports.</li>
</ul>
<h2 id="building">Building</h2>
<p>In case you have Java and Maven locally installed call:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># run all tests</span>
$ mvn verify
</code></pre></div><p>In case you have <code>ilo</code> installed, call this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># build the project</span>
$ ilo @build/once

<span class="c1"># open a shell with a pre-defined build environment</span>
$ ilo
</code></pre></div><p>In case you want to build the website do this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># build website</span>
$ hugo --minify --i18n-warnings --path-warnings --source docs

<span class="c1"># serve website</span>
$ hugo server --minify --i18n-warnings --path-warnings --source docs --watch
</code></pre></div><p>Take a look at the <a href="../makefile">Makefile</a> as an easy way to call all these commands.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/contributors" term="contributors" label="Contributors"/><category scheme="https://ilo.projects.metio.wtf/tags/build" term="build" label="build"/><category scheme="https://ilo.projects.metio.wtf/tags/environment" term="environment" label="environment"/></entry><entry><title type="html">CI Servers</title><link href="https://ilo.projects.metio.wtf/integration/ci-servers/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/integration/ci-servers/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<h2 id="jenkins">Jenkins</h2>
<p><a href="https://jenkins.io/">Jenkins</a> knows multiple ways to define jobs. In order to integrate with <code>ilo</code> make sure the <code>ilo</code> is <a href="../../usage/install">installed</a> on your slaves.</p>
<h3 id="declarative-pipeline">Declarative Pipeline</h3>
<div class="highlight"><pre class="chroma"><code class="language-groovy" data-lang="groovy"><span class="n">pipeline</span> <span class="o">{</span>
  <span class="n">stages</span> <span class="o">{</span>
    <span class="n">stage</span><span class="o">(</span><span class="s1">&#39;Build with ilo&#39;</span><span class="o">)</span> <span class="o">{</span>
      <span class="n">sh</span> <span class="s1">&#39;ilo @build-things&#39;</span>
    <span class="o">}</span>
  <span class="o">}</span>
<span class="o">}</span>
</code></pre></div><p><code>build-things</code> is an <a href="../../usage/argument-files">arguments</a> file.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/integration" term="integration" label="integration"/><category scheme="https://ilo.projects.metio.wtf/tags/jenkins" term="jenkins" label="jenkins"/></entry><entry><title type="html">CLI Autocomplete</title><link href="https://ilo.projects.metio.wtf/usage/cli-autocomplete/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/usage/cli-autocomplete/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The <code>ilo generate-completion</code> command generates autocompletion configuration for shells such as <a href="https://www.gnu.org/software/bash/">bash</a> and <a href="https://www.zsh.org/">zsh</a>.</p>
<p>In order to get autocomplete working in your shell, execute the following snippet:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ <span class="nb">source</span> &lt;<span class="o">(</span>ilo generate-completion<span class="o">)</span>
</code></pre></div><p>See the integration pages for various <a href="../../integration/shells">shells</a> for details.</p>
<p>Once enabled you can use the <code>&lt;TAB&gt;</code> key to autocomplete ilo commands and their options:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># autocomplete commands</span>
$ ilo s&lt;TAB&gt;
$ ilo shell

<span class="c1"># autocomplete options</span>
$ ilo shell --p&lt;TAB&gt;
$ ilo shell --pull
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/usage" term="usage" label="usage"/><category scheme="https://ilo.projects.metio.wtf/tags/autocomplete" term="autocomplete" label="autocomplete"/></entry><entry><title type="html">Container Registries</title><link href="https://ilo.projects.metio.wtf/integration/container-registries/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/integration/container-registries/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In order to share build environments within a team, using a <a href="https://github.com/opencontainers/distribution-spec">container registry</a> is highly recommended.</p>
<h2 id="docker-hub">Docker Hub</h2>
<p><a href="https://hub.docker.com/">Docker Hub</a> has an <a href="https://docs.docker.com/docker-hub/builds/">automated build system</a> that can be used to automatically create a build environment which in turn can be used by team members of your project.</p>
<p>Make sure Docker Hub rebuilds your build environment on every change to main (or any other branch) and have your contributors pull the resulting images to their machines.</p>
<h2 id="github-container-registry">GitHub Container Registry</h2>
<p>The <a href="https://github.blog/2020-09-01-introducing-github-container-registry/">GitHub container registry</a> can be used together with <a href="https://github.com/features/actions">GitHub actions</a> to publish OCI images for your team. Simply follow their getting started <a href="https://docs.github.com/en/packages/getting-started-with-github-container-registry/migrating-to-github-container-registry-for-docker-images">guide</a> to publish your image(s).</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/integration" term="integration" label="integration"/><category scheme="https://ilo.projects.metio.wtf/tags/docker-hub" term="docker-hub" label="docker hub"/><category scheme="https://ilo.projects.metio.wtf/tags/github-container-registry" term="github-container-registry" label="github container registry"/></entry><entry><title type="html">Customize Environment</title><link href="https://ilo.projects.metio.wtf/compose/customize-env/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/devcontainer/customize-env/?utm_source=atom_feed" rel="related" type="text/html" title="Customize Environment"/><link href="https://ilo.projects.metio.wtf/shell/customize-env/?utm_source=atom_feed" rel="related" type="text/html" title="Customize Environment"/><id>https://ilo.projects.metio.wtf/compose/customize-env/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The <code>ilo compose</code> command looks for a <code>docker-compose.yml</code> file by default and uses the <code>dev</code> service to open your new shell. Specify any option, additional volumes, custom images and commands in that file in order to customize your build environment according to your needs. Depending on your preferred <a href="../runtimes">runtime</a> you probably have to change the location of the file to use with the <code>--file</code> flag, e.g. <a href="https://github.com/abalage/pods-compose">pods-compose</a> will look for a <code>pods-compose.ini</code> instead.</p>
<h2 id="docker-compose">docker-compose</h2>
<p><code>docker-compose</code> is the default runtime for <code>ilo compose</code>. Take a look at the <a href="https://docs.docker.com/compose/compose-file/">reference documentation</a> for possible configuration values.</p>
<div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml"><span class="nt">version</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;3.8&#34;</span><span class="w">
</span><span class="w"></span><span class="nt">services</span><span class="p">:</span><span class="w">
</span><span class="w">  </span><span class="nt">redis</span><span class="p">:</span><span class="w">
</span><span class="w">    </span><span class="nt">image</span><span class="p">:</span><span class="w"> </span><span class="l">redis:latest</span><span class="w">
</span><span class="w">  </span><span class="nt">dev</span><span class="p">:</span><span class="w">
</span><span class="w">    </span><span class="nt">image</span><span class="p">:</span><span class="w"> </span><span class="l">openjdk:11</span><span class="w">
</span><span class="w">    </span><span class="nt">command</span><span class="p">:</span><span class="w"> </span><span class="l">/bin/bash        </span><span class="w"> </span><span class="c"># custom command</span><span class="w">
</span><span class="w">    </span><span class="nt">volumes</span><span class="p">:</span><span class="w">
</span><span class="w">      </span>- <span class="l">.:/some/where         </span><span class="w"> </span><span class="c"># mount project directory</span><span class="w">
</span><span class="w">    </span><span class="nt">working_dir</span><span class="p">:</span><span class="w"> </span><span class="l">/some/where  </span><span class="w"> </span><span class="c"># set working directory inside container</span><span class="w">
</span><span class="w">    </span><span class="nt">depends_on</span><span class="p">:</span><span class="w">
</span><span class="w">      </span>- <span class="l">redis                 </span><span class="w"> </span><span class="c"># auto-start build dependencies</span><span class="w">
</span></code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/compose" term="compose" label="compose"/><category scheme="https://ilo.projects.metio.wtf/tags/customize" term="customize" label="customize"/></entry><entry><title type="html">Customize Environment</title><link href="https://ilo.projects.metio.wtf/devcontainer/customize-env/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/compose/customize-env/?utm_source=atom_feed" rel="related" type="text/html" title="Customize Environment"/><link href="https://ilo.projects.metio.wtf/shell/customize-env/?utm_source=atom_feed" rel="related" type="text/html" title="Customize Environment"/><id>https://ilo.projects.metio.wtf/devcontainer/customize-env/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p><code>ilo</code> allows you to define your build environment either in a <a href="https://docs.docker.com/engine/reference/builder/">Dockerfile</a> or any other <a href="https://github.com/opencontainers/image-spec/blob/master/spec.md">OCI Image</a> compliant way.</p>
<p>Make sure to specify <code>your.image.here:some-tag</code> as the image in your <code>devcontainer.json</code> file. Take a look at the <a href="https://code.visualstudio.com/docs/remote/devcontainerjson-reference">reference documentation</a> for all available options for that JSON file.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/devcontainer" term="devcontainer" label="devcontainer"/><category scheme="https://ilo.projects.metio.wtf/tags/customize" term="customize" label="customize"/></entry><entry><title type="html">Customize Environment</title><link href="https://ilo.projects.metio.wtf/shell/customize-env/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/compose/customize-env/?utm_source=atom_feed" rel="related" type="text/html" title="Customize Environment"/><link href="https://ilo.projects.metio.wtf/devcontainer/customize-env/?utm_source=atom_feed" rel="related" type="text/html" title="Customize Environment"/><id>https://ilo.projects.metio.wtf/shell/customize-env/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The <code>ilo shell</code> command uses the <code>fedora:latest</code> image by default. In most cases <code>fedora:latest</code> will not be enough to compile/test/package/run your software. While you can install additional packages inside the container, those changes will be lost once you remove the container. In contrast to <a href="https://github.com/containers/toolbox">toolbox</a>, <code>ilo</code> relies on immutable containers.</p>
<p><code>ilo</code> allows you to define your build environment either in a <a href="https://docs.docker.com/engine/reference/builder/">Dockerfile</a> or any other <a href="https://github.com/opencontainers/image-spec/blob/master/spec.md">OCI Image</a> compliant way.</p>
<p>Make sure your image can be accessed by everyone in your team and use <code>ilo shell your.image.here:latest</code> to open a new instance of your build environment.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># write some Dockerfile</span>
$ cat Dockerfile
FROM maven:slim

RUN apt-get update <span class="o">&amp;&amp;</span> apt-get install hugo -y

CMD <span class="o">[</span><span class="s2">&#34;jshell&#34;</span><span class="o">]</span>
</code></pre></div><p>Build the image like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># build image</span>
$ docker build --tag your.image:latest .
</code></pre></div><p>Once completed, you can open a new shell like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># use image, jump into &#39;jshell&#39;</span>
$ ilo shell your.image:latest
</code></pre></div><h2 id="local-environments">Local environments</h2>
<p>In case you cannot push an image to a registry (e.g. you don&rsquo;t have one), you can use the <code>--dockerfile</code> option to let <code>ilo</code> build your image first before entering a new shell. The above example can thus be simplified to:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo shell --dockerfile your.dockerfile your.image:latest
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/shell" term="shell" label="shell"/><category scheme="https://ilo.projects.metio.wtf/tags/customize" term="customize" label="customize"/></entry><entry><title type="html">Editors</title><link href="https://ilo.projects.metio.wtf/contributors/editors/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/contributors/editors/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<h2 id="intellij">IntelliJ</h2>
<p><a href="https://www.jetbrains.com/idea/">IntelliJ Idea</a> is a popular IDE by <a href="https://www.jetbrains.com/">Jetbrains</a>. It allows <a href="https://www.jetbrains.com/help/idea/sharing-your-ide-settings.html">several ways</a> to share settings across team members in a project.</p>
<p><code>ilo</code> stores its IntelliJ settings inside its <a href="https://www.jetbrains.com/help/idea/creating-and-managing-projects.html#share-project-through-vcs">git repository</a> and thus should work out-of-the-box for other IntelliJ users.</p>
<p>In case you have trouble getting <code>ilo</code> to work in IntelliJ, take a look at the <a href="../../community/help">help</a> section.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/contributors" term="contributors" label="Contributors"/><category scheme="https://ilo.projects.metio.wtf/tags/editor" term="editor" label="editor"/><category scheme="https://ilo.projects.metio.wtf/tags/idea" term="idea" label="idea"/><category scheme="https://ilo.projects.metio.wtf/tags/ide" term="ide" label="ide"/></entry><entry><title type="html">Examples</title><link href="https://ilo.projects.metio.wtf/shell/examples/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/shell/examples/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The following examples show how <code>ilo shell</code> can be used.</p>
<h2 id="cargo-projects">Cargo Projects</h2>
<p><a href="https://doc.rust-lang.org/cargo/">Cargo</a> caches all downloaded dependencies in your local <code>~/.cargo</code> directory.</p>
<p>In order to re-use already downloaded dependencies inside the container, specify a <code>--volumne</code> like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># Gradle project that mounts local .gradle folder</span>
$ ilo shell <span class="se">\
</span><span class="se"></span>    --volume <span class="nv">$HOME</span>/.cargo:/root/.cargo:Z <span class="se">\
</span><span class="se"></span>    rust:latest
</code></pre></div><h2 id="gradle-projects">Gradle Projects</h2>
<p><a href="https://gradle.org/">Gradle</a> caches all downloaded dependencies in your local <code>~/.gradle</code> directory.</p>
<p>In order to re-use already downloaded dependencies inside the container, specify a <code>--volumne</code> like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># Gradle project that mounts local .gradle folder</span>
$ ilo shell <span class="se">\
</span><span class="se"></span>    --volume <span class="nv">$HOME</span>/.gradle:/home/gradle/.gradle:Z <span class="se">\
</span><span class="se"></span>    gradle:latest
</code></pre></div><h2 id="maven-projects">Maven Projects</h2>
<p><a href="https://maven.apache.org/">Maven</a> caches all downloaded dependencies in your local <code>~/.m2</code> directory.</p>
<p>In order to re-use already downloaded dependencies inside the container, specify a <code>--volumne</code> like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># Maven project that mounts local m2 repo</span>
$ ilo shell <span class="se">\
</span><span class="se"></span>    --volume <span class="nv">$HOME</span>/.m2:/root/.m2:Z <span class="se">\
</span><span class="se"></span>    maven:latest
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/shell" term="shell" label="shell"/><category scheme="https://ilo.projects.metio.wtf/tags/examples" term="examples" label="examples"/></entry><entry><title type="html">First Timer</title><link href="https://ilo.projects.metio.wtf/contributors/first-timer/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/community/help/?utm_source=atom_feed" rel="related" type="text/html" title="Help"/><link href="https://ilo.projects.metio.wtf/usage/help/?utm_source=atom_feed" rel="related" type="text/html" title="Help"/><link href="https://ilo.projects.metio.wtf/community/security/?utm_source=atom_feed" rel="related" type="text/html" title="Security"/><id>https://ilo.projects.metio.wtf/contributors/first-timer/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>First time contributors have to perform an additional step in order to get their changes merged into the main repository.</p>
<p>The <a href="https://github.com/metio/ilo/blob/main/AUTHORS/WAIVER">WAIVER</a> has to be signed in order to signal that all contributions can be published into the public domain using the <a href="https://creativecommons.org/publicdomain/zero/1.0/">Creative Commons Zero (CC0)</a> license.</p>
<p><a href="https://jedisct1.github.io/minisign/">minisign</a> is used to sign and verify contributions (see <a href="../dev-env">dev-env</a>).</p>
<p>The signed waiver <strong>must</strong> be added to your first pull request. The created git commit <strong>must</strong> be <a href="https://git-scm.com/docs/git-commit#Documentation/git-commit.txt--Sltkeyidgt">GPG signed</a>.</p>
<p>Additionally, any contributor may add her/his <a href="../metadata">metadata</a> to the project. This is especially useful for <code>minisign</code> public keys since you will have to communicate to the project maintainers anyway.</p>
<h2 id="prerequisite">Prerequisite</h2>
<p>Make sure at least version <code>0.8</code> of <code>minisign</code> is installed:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ minisign -v
minisign 0.8
</code></pre></div><h2 id="sign">Sign</h2>
<p>In order to sign the waiver, call the following commands from the root of the project:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ minisign -Sm AUTHORS/WAIVER
$ mv AUTHORS/WAIVER.minisig AUTHORS/WAIVER.<span class="sb">`</span>id --name --user<span class="sb">`</span>.minisig
</code></pre></div><p>In case you are into <a href="../makefile">Makefiles</a>, you can use the shorter version:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ make sign-waiver
</code></pre></div><p>In both cases, a new file called <code>AUTHORS/WAIVER.&lt;USER&gt;.minisign</code> was created. The <code>Makefile</code> will commit the signed waiver to your local repo as well. In case you have not used the <code>Makefile</code>, run this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ git add AUTHORS/WAIVER.<span class="sb">`</span>id --name --user<span class="sb">`</span>.minisig
$ git commit -m <span class="s1">&#39;sign waiver&#39;</span> --gpg-sign
</code></pre></div><h2 id="verify">Verify</h2>
<p>An existing contributor will verify your signature with:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ minisign -V -x AUTHORS/WAIVER.&lt;YOUR_NAME&gt;.minisig -P &lt;YOUR_PUB_KEY&gt; -m AUTHORS/WAIVER
</code></pre></div><p>In order to verify existing signatures, do any of the following:</p>



<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># verify signature: sebhoss</span>
$ minisign -V -x AUTHORS/WAIVER.sebhoss.minisig -P RWTwUvueDHNazzoOSJ/rbmIWhvMXIWBUJezoJhSE9jP/Nhv&#43;4SUz3HvS -m AUTHORS/WAIVER
</code></pre>
</div>


]]></content><category scheme="https://ilo.projects.metio.wtf/categories/contributors" term="contributors" label="Contributors"/><category scheme="https://ilo.projects.metio.wtf/tags/help" term="help" label="help"/></entry><entry><title type="html">Git Mirrors</title><link href="https://ilo.projects.metio.wtf/community/git-mirrors/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/community/git-mirrors/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p><code>ilo</code> is using <a href="https://github.com/metio/ilo">GitHub</a> as its primary source/issue/build environment, however its code is available in other git repositories as well in order to deal with <a href="https://www.githubstatus.com/">GitHub outages</a>.</p>
<h2 id="available-mirrors">Available Mirrors</h2>
<p>Mirrors are updated automatically as explained in this <a href="https://seb.people.metio.wtf/topic/gitlab-the-git-distributor/">blog post</a>.</p>
<ul>
<li><a href="https://github.com/metio/ilo">https://github.com/metio/ilo</a></li>
<li><a href="https://gitlab.com/metio.wtf/ilo">https://gitlab.com/metio.wtf/ilo</a></li>
<li><a href="https://codeberg.org/metio.wtf/ilo">https://codeberg.org/metio.wtf/ilo</a></li>
<li><a href="https://bitbucket.org/metio-wtf/ilo">https://bitbucket.org/metio-wtf/ilo</a></li>
<li><a href="https://repo.or.cz/ilo.git">https://repo.or.cz/ilo.git</a></li>
</ul>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/contributors" term="contributors" label="Contributors"/><category scheme="https://ilo.projects.metio.wtf/tags/git" term="git" label="git"/><category scheme="https://ilo.projects.metio.wtf/tags/mirror" term="mirror" label="mirror"/></entry><entry><title type="html">Help</title><link href="https://ilo.projects.metio.wtf/community/help/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/contributors/first-timer/?utm_source=atom_feed" rel="related" type="text/html" title="First Timer"/><link href="https://ilo.projects.metio.wtf/usage/help/?utm_source=atom_feed" rel="related" type="text/html" title="Help"/><link href="https://ilo.projects.metio.wtf/community/security/?utm_source=atom_feed" rel="related" type="text/html" title="Security"/><id>https://ilo.projects.metio.wtf/community/help/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In case you need help, don’t panic - we’ve all been there! Try the following resources in order to get help:</p>
<ul>
<li><a href="https://github.com/metio/ilo/issues/new?assignees=sebhoss&amp;labels=bug&amp;template=bug_report.md&amp;title=">open a new bug report</a></li>
<li><a href="https://github.com/metio/ilo/issues/new?assignees=&amp;labels=enhancement&amp;template=feature_request.md&amp;title=">create a new feature request</a></li>
<li><a href="https://github.com/metio/ilo/discussions">open a new discussion</a></li>
<li><a href="https://matrix.to/#/#ilo:matrix.org">join the chat room</a></li>
<li><a href="https://metio.groups.io/g/ilo/topics">send an email to the mailing list</a></li>
<li>Take a walk outside &amp; come back to your issue with a fresh mind</li>
</ul>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/community" term="community" label="Community"/><category scheme="https://ilo.projects.metio.wtf/tags/help" term="help" label="help"/></entry><entry><title type="html">Help</title><link href="https://ilo.projects.metio.wtf/usage/help/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/contributors/first-timer/?utm_source=atom_feed" rel="related" type="text/html" title="First Timer"/><link href="https://ilo.projects.metio.wtf/community/help/?utm_source=atom_feed" rel="related" type="text/html" title="Help"/><link href="https://ilo.projects.metio.wtf/community/security/?utm_source=atom_feed" rel="related" type="text/html" title="Security"/><id>https://ilo.projects.metio.wtf/usage/help/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In order to see the help information of <code>ilo</code>, run <code>ilo --help</code> or <code>ilo -h</code>. Its output looks something like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo --help
Usage: ilo <span class="o">[</span>-hV<span class="o">]</span> <span class="o">[</span>@&lt;filename&gt;...<span class="o">]</span> COMMAND

Manage reproducible build environments

      <span class="o">[</span>@&lt;filename&gt;...<span class="o">]</span>   One or more argument files containing options.

Options:
  -h, --help             Show this <span class="nb">help</span> message and exit.
  -V, --version          Print version information and exit.

Commands:
  shell                Opens an <span class="o">(</span>interactive<span class="o">)</span> shell <span class="k">for</span> your build environment
  compose              Open an <span class="o">(</span>interactive<span class="o">)</span> shell using podman-/docker-compose
  devcontainer         Open an <span class="o">(</span>interactive<span class="o">)</span> shell using devcontainer
  generate-completion  Generate bash/zsh completion script <span class="k">for</span> ilo.
</code></pre></div><p>Note that you can use show help infos for each command as well, e.g. like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo shell --help
Usage: ilo shell <span class="o">[</span>-hV<span class="o">]</span> <span class="o">[</span>--debug<span class="o">]</span> <span class="o">[</span>--<span class="o">[</span>no-<span class="o">]</span>interactive<span class="o">]</span> <span class="o">[</span>--<span class="o">[</span>no-<span class="o">]</span>mount-project-dir<span class="o">]</span> <span class="o">[</span>--pull<span class="o">]</span> <span class="o">[</span>--remove-image<span class="o">]</span> <span class="o">[</span>--dockerfile<span class="o">=</span>&lt;dockerfile&gt;<span class="o">]</span> <span class="o">[</span>--runtime<span class="o">=</span>&lt;runtime&gt;<span class="o">]</span> <span class="o">[</span>--env<span class="o">=</span>&lt;variables&gt;<span class="o">]</span>... <span class="o">[</span>--publish<span class="o">=</span>&lt;ports&gt;<span class="o">]</span>...
                 <span class="o">[</span>--runtime-build-option<span class="o">=</span>&lt;runtimeBuildOptions&gt;<span class="o">]</span>... <span class="o">[</span>--runtime-cleanup-option<span class="o">=</span>&lt;runtimeCleanupOptions&gt;<span class="o">]</span>... <span class="o">[</span>--runtime-option<span class="o">=</span>&lt;runtimeOptions&gt;<span class="o">]</span>... <span class="o">[</span>--runtime-pull-option<span class="o">=</span>&lt;runtimePullOptions&gt;<span class="o">]</span>...
                 <span class="o">[</span>--runtime-run-option<span class="o">=</span>&lt;runtimeRunOptions&gt;<span class="o">]</span>... <span class="o">[</span>--volume<span class="o">=</span>&lt;volumes&gt;<span class="o">]</span>... &lt;image&gt; <span class="o">[</span>&lt;commands&gt;...<span class="o">]</span>

Opens an <span class="o">(</span>interactive<span class="o">)</span> shell <span class="k">for</span> your build environment

      &lt;image&gt;               The OCI image to use. In <span class="k">case</span> --dockerfile is given as well, this defines the name of the resulting image.
                              Default: fedora:latest
      <span class="o">[</span>&lt;commands&gt;...<span class="o">]</span>
      --debug               Show additional debug information.
      --dockerfile<span class="o">=</span>&lt;dockerfile&gt;
                            The Dockerfile to use.
      --env<span class="o">=</span>&lt;variables&gt;     Specify a environment variable <span class="k">for</span> the container.
  -h, --help                Show this <span class="nb">help</span> message and exit.
      --<span class="o">[</span>no-<span class="o">]</span>interactive    Open interactive shell or just run a single command.
      --<span class="o">[</span>no-<span class="o">]</span>mount-project-dir
                            Mount the project directory into the running container.
      --publish<span class="o">=</span>&lt;ports&gt;     Publish container ports to the host system.
      --pull                Pull image before opening shell.
      --remove-image        Remove image after closing the shell.
      --runtime<span class="o">=</span>&lt;runtime&gt;   Specify the runtime to use. If none is specified, use auto-selection.
      --runtime-build-option<span class="o">=</span>&lt;runtimeBuildOptions&gt;
                            Options <span class="k">for</span> the build <span class="nb">command</span> of the selected runtime.
      --runtime-cleanup-option<span class="o">=</span>&lt;runtimeCleanupOptions&gt;
                            Options <span class="k">for</span> the cleanup <span class="nb">command</span> of the selected runtime.
      --runtime-option<span class="o">=</span>&lt;runtimeOptions&gt;
                            Options <span class="k">for</span> the selected runtime itself.
      --runtime-pull-option<span class="o">=</span>&lt;runtimePullOptions&gt;
                            Options <span class="k">for</span> the pull <span class="nb">command</span> of the selected runtime.
      --runtime-run-option<span class="o">=</span>&lt;runtimeRunOptions&gt;
                            Options <span class="k">for</span> the run <span class="nb">command</span> of the selected runtime.
  -V, --version             Print version information and exit.
      --volume<span class="o">=</span>&lt;volumes&gt;    Mount a volume into the container.
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/usage" term="usage" label="usage"/><category scheme="https://ilo.projects.metio.wtf/tags/help" term="help" label="help"/></entry><entry><title type="html">Install</title><link href="https://ilo.projects.metio.wtf/usage/install/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/usage/install/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>Use the following locations in order to download the binary distribution of <code>ilo</code>:</p>
<ul>
<li><a href="https://github.com/metio/ilo/releases">https://github.com/metio/ilo/releases</a></li>
</ul>
<p>None of the widely used package managers have <code>ilo</code> in their official repositories yet. In case you want to help package <code>ilo</code> for your preferred operating system, take a look at the open packaging issues for <a href="https://github.com/metio/ilo/issues/2">Linux</a>, <a href="https://github.com/metio/ilo/issues/12">MacOS</a>, and <a href="https://github.com/metio/ilo/issues/11">Windows</a>.</p>
<h2 id="linux">Linux</h2>
<h3 id="manual">Manual</h3>
<p>The <a href="https://www.kernel.org/">Linux</a> distribution of <code>ilo</code> is a native executable and does not require any additional software to be installed.</p>


<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ cd /path/to/ilo/installation/folder
$ curl --location https://github.com/metio/ilo/releases/download/2020.10.12/ilo-2020.10.12-linux.zip --output ilo.zip
$ unzip ilo.zip
$ chmod +x ilo-2020.10.12/ilo
$ ln --symbolic --relative ilo-2020.10.12/ilo ~/.local/bin/ilo
</code></pre>
</div>

<h3 id="fedora">Fedora</h3>
<p><a href="https://getfedora.org/">Fedora</a> users can install <code>ilo</code> from a <a href="https://copr.fedorainfracloud.org/coprs/sebhoss/ilo/">COPR</a> repository like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ sudo dnf copr <span class="nb">enable</span> sebhoss/ilo
$ sudo dnf install ilo
</code></pre></div><h3 id="fedora-silverblue">Fedora Silverblue</h3>
<p><a href="https://silverblue.fedoraproject.org/">Fedora Silverblue</a> users can install <code>ilo</code> from the same <a href="https://copr.fedorainfracloud.org/coprs/sebhoss/ilo/">COPR</a> by following these steps:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ curl --location https://copr.fedorainfracloud.org/coprs/sebhoss/ilo/repo/fedora-<span class="k">$(</span>rpm -E %fedora<span class="k">)</span>/sebhoss-ilo-fedora-<span class="k">$(</span>rpm -E %fedora<span class="k">)</span>.repo --output ilo.repo
$ sudo mv ilo.repo /etc/yum.repos.d/ilo.repo
$ rpm-ostree install ilo
</code></pre></div><h2 id="macos">MacOS</h2>
<p>The <a href="https://www.apple.com/macos/">MacOS</a> distribution of <code>ilo</code> is a native executable and does not require any additional software to be installed.</p>


<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ cd /path/to/ilo/installation/folder
$ curl --location https://github.com/metio/ilo/releases/download/2020.10.12/ilo-2020.10.12-mac.zip --output ilo.zip
$ unzip ilo.zip
$ chmod +x ilo-2020.10.12/ilo
$ ln -s /path/to/ilo/installation/folder/ilo-2020.10.12/ilo /usr/local/bin/ilo
<span class="c1"># in case the above gives you a permission denied error, execute the same command with sudo, e.g. 'sudo !!'</span>
<span class="c1"># in order to fix permissions run: sudo chmod -R u=rwX,go=rX /usr/local/bin</span>
</code></pre>
</div>

<h2 id="windows">Windows</h2>
<p>The <a href="https://www.microsoft.com/en-us/windows">Windows</a> distribution of <code>ilo</code> is a native executable and does not require any additional software to be installed.</p>


<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># PowerShell</span>
$ New-Item -Path $env:USERPROFILE -Name "bin" -ItemType "directory" <span class="c1"># create directory if it does not exit</span>
$ control sysdm.cpl <span class="c1"># manually add '$env:USERPROFILE\bin' to the PATH of your user account</span>
$ echo $env:PATH <span class="c1"># ensure that the 'bin' directory is in your PATH</span>
$ Set-Location -Path C:\path\to\ilo\installation\folder
$ Invoke-RestMethod -Uri https://github.com/metio/ilo/releases/download/2020.10.12/ilo-2020.10.12-windows.zip -OutFile "ilo.zip"
$ Expand-Archive -Path "ilo.zip" -DestinationPath "."
$ Copy-Item -Path "ilo-2020.10.12\ilo.exe" -Destination "$env:USERPROFILE\bin\ilo.exe"
</code></pre>
</div>

<h2 id="other">Other</h2>
<p>For all other platforms that support at least <a href="https://www.oracle.com/javadownload">Java 11</a>, run something like this:</p>


<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ cd /path/to/ilo/installation/folder
$ curl --location https://github.com/metio/ilo/releases/download/2020.10.12/ilo-2020.10.12-java11.zip --output ilo.zip
$ unzip ilo.zip
$ chmod +x ilo-2020.10.12/ilo
<span class="c1"># move 'ilo' start script to your preferred location</span>
</code></pre>
</div>

]]></content><category scheme="https://ilo.projects.metio.wtf/categories/usage" term="usage" label="usage"/><category scheme="https://ilo.projects.metio.wtf/tags/install" term="install" label="install"/></entry><entry><title type="html">Makefile</title><link href="https://ilo.projects.metio.wtf/contributors/makefile/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/contributors/makefile/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The <a href="https://github.com/metio/ilo/blob/main/Makefile">Makefile</a> commonly used task by maintainers of this project. In order to use it, you have to install a variant of <a href="https://en.wikipedia.org/wiki/Make_(software)">make</a> on your system. Just calling <code>make</code> in the root directory will print the help/usage guide:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ make
usage: make <span class="o">[</span>target<span class="o">]</span>

contributing:
  sign-waiver                     Sign the WAIVER

example:
  ec-redis-java11                 Example using <span class="s1">&#39;ilo compose&#39;</span> w/ redis <span class="p">&amp;</span> java <span class="m">11</span>
  es-default                      Example using <span class="s1">&#39;ilo shell&#39;</span> w/ default settings
  es-openjdk11                    Example using <span class="s1">&#39;ilo shell&#39;</span> w/ openjdk11
  es-maven                        Example using <span class="s1">&#39;ilo shell&#39;</span> w/ maven

hacking:
  build                           Build everything
  native-image                    Create a native image using GraalVM
  clean                           Clean build artifacts

other:
  <span class="nb">help</span>                            Show this <span class="nb">help</span>
</code></pre></div><p>Run any of the available targets like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ make verify
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/contributors" term="contributors" label="Contributors"/><category scheme="https://ilo.projects.metio.wtf/tags/makefile" term="makefile" label="makefile"/></entry><entry><title type="html">Metadata</title><link href="https://ilo.projects.metio.wtf/contributors/metadata/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/contributors/metadata/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>Every contributor may add/remove her/his metadata to the <a href="https://github.com/metio/ilo/tree/main/docs/data/contributors">list of contributors</a> at any time.</p>
<p>Metadata is currently used in three places:</p>
<ol>
<li>To generate a list of existing <a href="https://jedisct1.github.io/minisign/">minisign</a> signatures in the <a href="../first-timer">first timer</a> documentation.</li>
<li>To generate a <a href="http://humanstxt.org/">humans.txt</a> file of <a href="https://ilo.projects.metio.wtf/humans.txt">all contributors</a>.</li>
<li>To generate a <a href="http://www.foaf-project.org/">FOAF</a> for the <a href="https://ilo.projects.metio.wtf/foaf.rdf">entire project</a>.</li>
</ol>
<h2 id="adding-a-new-entry">Adding a new entry</h2>
<p>Create a new file called <code>&lt;YOUR_NAME&gt;.yaml</code> in the <a href="https://github.com/metio/ilo/tree/main/docs/data/contributors">contributors directory</a>. Add the following properties to it:</p>
<div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml"><span class="nt">id</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;&lt;YOUR_NAME&gt;&#39;</span><span class="w">                   </span><span class="c"># should match file name (required)</span><span class="w">
</span><span class="w"></span><span class="nt">minisign</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;&lt;YOUR_MINISIGN_PUB_KEY&gt;&#39;</span><span class="w"> </span><span class="c"># used for key verification (required)</span><span class="w">
</span><span class="w"></span><span class="nt">title</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;&lt;YOUR_TITLE&gt;&#39;</span><span class="w">               </span><span class="c"># used by FOAF/humans.txt (optional)</span><span class="w">
</span><span class="w"></span><span class="nt">first_name</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;&lt;YOUR_FIRST_NAME&gt;&#39;</span><span class="w">     </span><span class="c"># used by FOAF/humans.txt (optional)</span><span class="w">
</span><span class="w"></span><span class="nt">last_name</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;&lt;YOUR_LAST_NAME&gt;&#39;</span><span class="w">       </span><span class="c"># used by FOAF/humans.txt (optional)</span><span class="w">
</span><span class="w"></span><span class="nt">email</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;&lt;YOUR_EMAIL&gt;&#39;</span><span class="w">               </span><span class="c"># used by FOAF (optional)</span><span class="w">
</span><span class="w"></span><span class="nt">website</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;&lt;YOUR_URL&gt;&#39;</span><span class="w">               </span><span class="c"># used by FOAF/humans.txt (optional)</span><span class="w">
</span></code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/contributors" term="contributors" label="Contributors"/><category scheme="https://ilo.projects.metio.wtf/tags/metadata" term="metadata" label="metadata"/></entry><entry><title type="html">News</title><link href="https://ilo.projects.metio.wtf/community/news/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/community/news/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In order to stay up-to-date, consider the following resources:</p>
<ul>
<li><a href="https://matrix.to/#/#ilo:matrix.org">Join the chat room</a></li>
<li><a href="https://metio.groups.io/g/ilo/topics">Join the mailing list</a></li>
<li><a href="https://github.com/metio/ilo/releases.atom">Subscribe to the release feed</a></li>
</ul>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/community" term="community" label="Community"/><category scheme="https://ilo.projects.metio.wtf/tags/news" term="news" label="news"/></entry><entry><title type="html">Options</title><link href="https://ilo.projects.metio.wtf/compose/options/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/shell/options/?utm_source=atom_feed" rel="related" type="text/html" title="Options"/><link href="https://ilo.projects.metio.wtf/devcontainer/options/?utm_source=atom_feed" rel="related" type="text/html" title="Options"/><id>https://ilo.projects.metio.wtf/compose/options/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The <code>ilo compose</code> command can be configured with the following command line options. From your terminal, use <code>ilo compose --help</code> to get a list of all options, and their default values.</p>
<h2 id="--build"><code>--build</code></h2>
<p>The <code>--build</code> option can be used to build images before opening a new shell. Use this option in case you rely on local Dockerfiles.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># build images before opening shell</span>
$ ilo compose --build

<span class="c1"># do not build images before opening shell</span>
$ ilo compose --build<span class="o">=</span><span class="nb">false</span>

<span class="c1"># do not build images before opening shell</span>
$ ilo compose
</code></pre></div><p>By default, <code>--build</code> is not enabled.</p>
<h2 id="--debug"><code>--debug</code></h2>
<p>The <code>--debug</code> option toggles whether <code>ilo</code> should print the runtime commands into your terminal before executing them. This can be useful in case you want to move away from <code>ilo</code> and just use your preferred runtime instead.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># print runtime commands</span>
$ ilo compose --debug

<span class="c1"># do not print runtime commands</span>
$ ilo compose --debug<span class="o">=</span><span class="nb">false</span>
$ ilo compose
</code></pre></div><p>By default, <code>--debug</code> is not enabled.</p>
<h2 id="--file"><code>--file</code></h2>
<p>The <code>--file</code> option can be used to specify the file that contains your container definitions, e.g. a <code>docker-compose.yml</code> file.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># use custom location</span>
$ ilo compose --file /path/to/some/file.yaml

<span class="c1"># use default location</span>
$ ilo compose
</code></pre></div><p>By default, <code>--file</code> is set to <code>docker-compose.yml</code>.</p>
<h2 id="--interactive"><code>--interactive</code></h2>
<p>The <code>--interactive</code> option can be used to control whether you want an interactive session (default) or just want to execute a single command (e.g. during CI builds).</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># run interactively</span>
$ ilo compose --interactive

<span class="c1"># run non-interactive</span>
$ ilo compose --no-interactive
$ ilo compose --interactive<span class="o">=</span><span class="nb">false</span>

<span class="c1"># run interactively</span>
$ ilo compose
</code></pre></div><p>By default, <code>--interactive</code> is enabled.</p>
<h2 id="--pull"><code>--pull</code></h2>
<p>The <code>--pull</code> option can be used to pull images before opening a new shell. This is especially useful for teams using a <code>latest</code> tag for their image. The image will only pulled in case the registry contains a newer image than locally available.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># pull image before opening shell</span>
$ ilo compose --pull

<span class="c1"># do not pull image before opening shell</span>
$ ilo compose --pull<span class="o">=</span><span class="nb">false</span>

<span class="c1"># do not pull image before opening shell</span>
$ ilo compose
</code></pre></div><p>By default, <code>--pull</code> is not enabled.</p>
<h2 id="--runtime"><code>--runtime</code></h2>
<p>The <code>--runtime</code> option can be used to force the usage of a specific runtime. See <a href="../runtimes">runtimes</a> for details.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># force to use docker-compose</span>
$ ilo compose --runtime docker-compose

<span class="c1"># force to use podman-compose</span>
$ ilo compose --runtime podman-compose

<span class="c1"># force to use pods-compose</span>
$ ilo compose --runtime pods-compose

<span class="c1"># force to use footloose</span>
$ ilo compose --runtime footloose

<span class="c1"># force to use vagrant</span>
$ ilo compose --runtime vagrant

<span class="c1"># auto select</span>
$ ilo compose
</code></pre></div><p>In case no <code>--runtime</code> is specified, <code>ilo compose</code> will automatically select one of the runtimes installed on your local system and prefers <code>docker-compose</code> over <code>podman-compose</code> over <code>pods-compose</code> over <code>footloose</code> over <code>vagrant</code> at the moment.</p>
<h2 id="--runtime-option"><code>--runtime-option</code></h2>
<p>The <code>--runtime-option</code> option can be specified multiple times and contains options for the underlying runtime.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo compose --runtime-option<span class="o">=</span>--no-ansi
$ ilo compose --runtime-option<span class="o">=</span>--no-ansi --runtime-option<span class="o">=</span>--tls

<span class="c1"># do not specify custom option</span>
$ ilo compose
</code></pre></div><p>By default, <code>--runtime-option</code> is set to an empty array.</p>
<h2 id="--runtime-pull-option"><code>--runtime-pull-option</code></h2>
<p>The <code>--runtime-pull-option</code> can be specified multiple times and contains options for the runtimes' <code>pull</code> command.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo compose --runtime-pull-option<span class="o">=</span>--ignore-pull-failures
$ ilo compose --runtime-pull-option<span class="o">=</span>--ignore-pull-failures --runtime-pull-option<span class="o">=</span>--quiet

<span class="c1"># do not specify custom option</span>
$ ilo compose
</code></pre></div><p>By default, <code>--runtime-pull-option</code> is set to an empty array.</p>
<h2 id="--runtime-build-option"><code>--runtime-build-option</code></h2>
<p>The <code>--runtime-build-option</code> can be specified multiple times and contains options for the runtimes' <code>build</code> command.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo compose --runtime-build-option<span class="o">=</span>--compress
$ ilo compose --runtime-build-option<span class="o">=</span>--compress --runtime-build-option<span class="o">=</span>--no-cache

<span class="c1"># do not specify custom option</span>
$ ilo compose
</code></pre></div><p>By default, <code>--runtime-build-option</code> is set to an empty array.</p>
<h2 id="--runtime-run-option"><code>--runtime-run-option</code></h2>
<p>The <code>--runtime-run-option</code> can be specified multiple times and contains options for the runtimes' <code>run</code> command.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo compose --runtime-run-option<span class="o">=</span>--no-deps
$ ilo compose --runtime-run-option<span class="o">=</span>--no-deps --runtime-run-option<span class="o">=</span>--use-aliases

<span class="c1"># do not specify custom option</span>
$ ilo compose
</code></pre></div><p>By default, <code>--runtime-run-option</code> is set to an empty array.</p>
<h2 id="--runtime-cleanup-option"><code>--runtime-cleanup-option</code></h2>
<p>The <code>--runtime-cleanup-option</code> can be specified multiple times and contains options for the runtimes' cleanup command, e.g. <code>down</code> for <code>docker-compose</code>.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo compose --runtime-cleanup-option<span class="o">=</span>--remove-orphans
$ ilo compose --runtime-cleanup-option<span class="o">=</span>--remove-orphans --runtime-cleanup-option<span class="o">=</span>--volumes

<span class="c1"># do not specify custom option</span>
$ ilo compose
</code></pre></div><p>By default, <code>--runtime-cleanup-option</code> is set to an empty array.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/compose" term="compose" label="compose"/><category scheme="https://ilo.projects.metio.wtf/tags/options" term="options" label="options"/></entry><entry><title type="html">Options</title><link href="https://ilo.projects.metio.wtf/devcontainer/options/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/compose/options/?utm_source=atom_feed" rel="related" type="text/html" title="Options"/><link href="https://ilo.projects.metio.wtf/shell/options/?utm_source=atom_feed" rel="related" type="text/html" title="Options"/><id>https://ilo.projects.metio.wtf/devcontainer/options/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The <code>ilo devcontainer</code> command can be configured with the following command line options. From your terminal, use <code>ilo compose --help</code> to get a list of all options, and their default values.</p>
<h2 id="--debug"><code>--debug</code></h2>
<p>The <code>--debug</code> option toggles whether <code>ilo</code> should print the runtime commands into your terminal before executing them. This can be useful in case you want to move away from <code>ilo</code> and just use your preferred runtime instead.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># print runtime commands</span>
$ ilo devcontainer --debug

<span class="c1"># do not print runtime commands</span>
$ ilo devcontainer --debug<span class="o">=</span><span class="nb">false</span>
$ ilo devcontainer
</code></pre></div><p>By default, <code>--debug</code> is not enabled.</p>
<h2 id="--mount-project-dir"><code>--mount-project-dir</code></h2>
<p>The <code>--mount-project-dir</code> option can be used to toggle whether the current directory should be mounted into the container.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># mount current directory into container</span>
$ ilo devcontainer --mount-project-dir

<span class="c1"># do not mount directory</span>
$ ilo devcontainer --no-mount-project-dir
$ ilo devcontainer --mount-project-dir<span class="o">=</span><span class="nb">false</span>

<span class="c1"># mount directory</span>
$ ilo devcontainer
</code></pre></div><p>By default, <code>--mount-project-dir</code> is enabled.</p>
<h2 id="--pull"><code>--pull</code></h2>
<p>The <code>--pull</code> option can be used to pull the specified image before opening a new shell. This is especially useful for teams using a <code>latest</code> tag for their image. The image will only pulled in case the registry contains a newer image than locally available.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># pull image before opening shell</span>
$ ilo devcontainer --pull

<span class="c1"># do not pull image before opening shell</span>
$ ilo devcontainer --no-pull
$ ilo devcontainer --pull<span class="o">=</span><span class="nb">false</span>

<span class="c1"># do not pull image before opening shell</span>
$ ilo devcontainer
</code></pre></div><p>By default, <code>--pull</code> is not enabled.</p>
<h2 id="--remove-image"><code>--remove-image</code></h2>
<p>The <code>--remove-image</code> option causes <code>ilo</code> to remove the specified image from your local system after you close your shell, or the non-interactive commands finishes.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># remove image after shell was closed</span>
$ ilo devcontainer --remove-image

<span class="c1"># do not remove image</span>
$ ilo devcontainer --no-remove-image
$ ilo devcontainer --remove-image<span class="o">=</span><span class="nb">false</span>
$ ilo devcontainer
</code></pre></div><p>By default, <code>--remove-image</code> is not enabled.</p>
<h2 id="--compose-runtime---c"><code>--compose-runtime</code> / <code>-C</code></h2>
<p>The <code>--compose-runtime</code> option can be used to force the usage of a specific <a href="../../compose/runtimes">compose runtime</a>.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># force to use podman-compose</span>
$ ilo devcontainer --compose-runtime podman-compose

<span class="c1"># force to use docker-compose</span>
$ ilo devcontainer --compose-runtime docker-compose

<span class="c1"># auto select</span>
$ ilo devcontainer
</code></pre></div><p>In case no <code>--compose-runtime</code> is specified, <code>ilo devcontainer</code> will automatically select one of the runtimes installed on your local system and prefers <code>docker-compose</code> over <code>podman-compose</code>.</p>
<h2 id="--shell-runtime---s"><code>--shell-runtime</code> / <code>-S</code></h2>
<p>The <code>--shell-runtime</code> option can be used to force the usage of a specific <a href="../../shell/runtimes">shell runtime</a>.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># force to use podman</span>
$ ilo devcontainer --shell-runtime podman

<span class="c1"># force to use docker</span>
$ ilo devcontainer --shell-runtime docker

<span class="c1"># force to use lxd</span>
$ ilo devcontainer --shell-runtime lxd

<span class="c1"># auto select</span>
$ ilo devcontainer
</code></pre></div><p>In case no <code>--shell-runtime</code> is specified, <code>ilo devcontainer</code> will automatically select one of the runtimes installed on your local system and prefers <code>podman</code> over <code>docker</code> over <code>lxd</code>.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/devcontainer" term="devcontainer" label="devcontainer"/><category scheme="https://ilo.projects.metio.wtf/tags/options" term="options" label="options"/></entry><entry><title type="html">Options</title><link href="https://ilo.projects.metio.wtf/shell/options/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/devcontainer/options/?utm_source=atom_feed" rel="related" type="text/html" title="Options"/><link href="https://ilo.projects.metio.wtf/compose/options/?utm_source=atom_feed" rel="related" type="text/html" title="Options"/><id>https://ilo.projects.metio.wtf/shell/options/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The <code>ilo shell</code> command can be configured with the following command line options. From your terminal, use <code>ilo shell --help</code> to get a list of all options, and their default values.</p>
<h2 id="--context"><code>--context</code></h2>
<p>The <code>--context</code> option allows to specify the context when building an image with <code>--dockerfile</code>.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom context</span>
$ ilo shell --context ./local/folder

<span class="c1"># do not specify context</span>
$ ilo shell
</code></pre></div><p>By default, <code>--context</code> is set to <code>.</code> (the current directory).</p>
<h2 id="--debug"><code>--debug</code></h2>
<p>The <code>--debug</code> option toggles whether <code>ilo</code> should print the runtime commands into your terminal before executing them. This can be useful in case you want to move away from <code>ilo</code> and just use your preferred runtime instead.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># print runtime commands</span>
$ ilo shell --debug

<span class="c1"># do not print runtime commands</span>
$ ilo shell --debug<span class="o">=</span><span class="nb">false</span>
$ ilo shell
</code></pre></div><p>By default, <code>--debug</code> is not enabled.</p>
<h2 id="--dockerfile"><code>--dockerfile</code></h2>
<p>The <code>--dockerfile</code> can be used to specify a local <code>Dockerfile</code>. Specifying such a file will cause <code>ilo</code> to build your image first, and then open a shell into a container based on that image.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom Dockerfile</span>
$ ilo shell --dockerfile some/where/your.dockerfile

<span class="c1"># do not specify Dockerfile</span>
$ ilo shell
</code></pre></div><p>By default, <code>--dockerfile</code> is not set to any value.</p>
<h2 id="--env"><code>--env</code></h2>
<p>The <code>--env</code> option can be used to specify environment variables for your container.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify env variable</span>
$ ilo shell --env <span class="nv">key</span><span class="o">=</span>value

<span class="c1"># do not specify env variable</span>
$ ilo shell
</code></pre></div><p>By default, <code>--env</code> does not set any environment variables.</p>
<h2 id="--interactive"><code>--interactive</code></h2>
<p>The <code>--interactive</code> option can be used to control whether you want an interactive session (default) or just want to execute a single command (e.g. during CI builds).</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># run interactively</span>
$ ilo shell --interactive

<span class="c1"># run non-interactive</span>
$ ilo shell --no-interactive
$ ilo shell --interactive<span class="o">=</span><span class="nb">false</span>

<span class="c1"># run interactively</span>
$ ilo shell
</code></pre></div><p>By default, <code>--interactive</code> is enabled.</p>
<h2 id="--mount-project-dir"><code>--mount-project-dir</code></h2>
<p>The <code>--mount-project-dir</code> option can be used to toggle whether the current directory should be mounted into the container.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># mount current directory into container</span>
$ ilo shell --mount-project-dir

<span class="c1"># do not mount directory</span>
$ ilo shell --no-mount-project-dir
$ ilo shell --mount-project-dir<span class="o">=</span><span class="nb">false</span>

<span class="c1"># mount directory</span>
$ ilo shell
</code></pre></div><p>By default, <code>--mount-project-dir</code> is enabled.</p>
<h2 id="--publish"><code>--publish</code></h2>
<p>The <code>--publish</code> option can be used to publish ports of your container to your local system.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># binds your local 8080 port to port 80 of the container</span>
$ ilo shell --publish 8080:80

<span class="c1"># do not expose any ports</span>
$ ilo shell
</code></pre></div><p>By default, <code>--publish</code> does not exposes any ports.</p>
<h2 id="--pull"><code>--pull</code></h2>
<p>The <code>--pull</code> option can be used to pull the specified image before opening a new shell. This is especially useful for teams using a <code>latest</code> tag for their image. The image will only pulled in case the registry contains a newer image than locally available.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># pull image before opening shell</span>
$ ilo shell --pull

<span class="c1"># do not pull image before opening shell</span>
$ ilo shell --no-pull
$ ilo shell --pull<span class="o">=</span><span class="nb">false</span>

<span class="c1"># do not pull image before opening shell</span>
$ ilo shell
</code></pre></div><p>By default, <code>--pull</code> is not enabled.</p>
<h2 id="--remove-image"><code>--remove-image</code></h2>
<p>The <code>--remove-image</code> option causes <code>ilo</code> to remove the specified image from your local system after you close your shell, or the non-interactive commands finishes. This is especially useful in combination with <code>--dockerfile</code>.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># remove image after shell was closed</span>
$ ilo shell --remove-image

<span class="c1"># do not remove image</span>
$ ilo shell --no-remove-image
$ ilo shell --remove-image<span class="o">=</span><span class="nb">false</span>
$ ilo shell
</code></pre></div><p>By default, <code>--remove-image</code> is not enabled.</p>
<h2 id="--runtime"><code>--runtime</code></h2>
<p>The <code>--runtime</code> option can be used to force the usage of a specific runtime. See <a href="../runtimes">runtimes</a> for details.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># force to use podman</span>
$ ilo shell --runtime podman

<span class="c1"># force to use docker</span>
$ ilo shell --runtime docker

<span class="c1"># force to use lxd</span>
$ ilo shell --runtime lxd

<span class="c1"># auto select</span>
$ ilo shell
</code></pre></div><p>In case no <code>--runtime</code> is specified, <code>ilo shell</code> will automatically select one of the runtimes installed on your local system and prefers <code>podman</code> over <code>docker</code> over <code>lxd</code>.</p>
<h2 id="--run-as"><code>--run-as</code></h2>
<p>The <code>--run-as</code> option can be used to override the user inside the container. This is helpful in case you are not able to run the rootless mode of your runtime.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># run as specified user:group</span>
$ ilo shell --run-as<span class="o">=</span><span class="k">$(</span>id -u<span class="k">)</span>:<span class="k">$(</span>id -g<span class="k">)</span>

<span class="c1"># run as user specified in image</span>
$ ilo shell
</code></pre></div><p>By default, <code>--run-as</code> is not set to any value.</p>
<h2 id="--runtime-option"><code>--runtime-option</code></h2>
<p>The <code>--runtime-option</code> option can be specified multiple times and contains options for the underlying runtime.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo shell --runtime-option<span class="o">=</span>--remote
$ ilo shell --runtime-option<span class="o">=</span>--remote --runtime-option<span class="o">=</span>--syslog

<span class="c1"># do not specify custom option</span>
$ ilo shell
</code></pre></div><p>By default, <code>--runtime-option</code> is set to an empty array.</p>
<h2 id="--runtime-pull-option"><code>--runtime-pull-option</code></h2>
<p>The <code>--runtime-pull-option</code> can be specified multiple times and contains options for the runtimes' <code>pull</code> command.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo shell --runtime-pull-option<span class="o">=</span>--all-tags
$ ilo shell --runtime-pull-option<span class="o">=</span>--all-tags --runtime-pull-option<span class="o">=</span>--quiet

<span class="c1"># do not specify custom option</span>
$ ilo shell
</code></pre></div><p>By default, <code>--runtime-pull-option</code> is set to an empty array.</p>
<h2 id="--runtime-build-option"><code>--runtime-build-option</code></h2>
<p>The <code>--runtime-build-option</code> can be specified multiple times and contains options for the runtimes' <code>build</code> command.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo shell --runtime-build-option<span class="o">=</span>--squash
$ ilo shell --runtime-build-option<span class="o">=</span>--squash --runtime-build-option<span class="o">=</span>--no-cache

<span class="c1"># do not specify custom option</span>
$ ilo shell
</code></pre></div><p>By default, <code>--runtime-build-option</code> is set to an empty array.</p>
<h2 id="--runtime-run-option"><code>--runtime-run-option</code></h2>
<p>The <code>--runtime-run-option</code> can be specified multiple times and contains options for the runtimes' <code>run</code> command.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo shell --runtime-run-option<span class="o">=</span>--privileged
$ ilo shell --runtime-run-option<span class="o">=</span>--privileged --runtime-run-option<span class="o">=</span>--replace

<span class="c1"># do not specify custom option</span>
$ ilo shell
</code></pre></div><p>By default, <code>--runtime-run-option</code> is set to an empty array.</p>
<h2 id="--runtime-cleanup-option"><code>--runtime-cleanup-option</code></h2>
<p>The <code>--runtime-cleanup-option</code> can be specified multiple times and contains options for the runtimes' <code>rmi</code> command.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># specify custom option(s)</span>
$ ilo shell --runtime-cleanup-option<span class="o">=</span>--force
$ ilo shell --runtime-cleanup-option<span class="o">=</span>--force --runtime-cleanup-option<span class="o">=</span>--all

<span class="c1"># do not specify custom option</span>
$ ilo shell
</code></pre></div><p>By default, <code>--runtime-cleanup-option</code> is set to an empty array.</p>
<h2 id="--volume"><code>--volume</code></h2>
<p>The <code>--volume</code> option can be used to mount additional volumes into your container.</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># mount extra volume</span>
$ ilo shell --volume <span class="nv">$HOME</span>/.m2/repository:/root/.m2/repository:Z

<span class="c1"># do not mount extra volume</span>
$ ilo shell
</code></pre></div><p>By default, <code>--volume</code> does not mount any extra volumes.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/shell" term="shell" label="shell"/><category scheme="https://ilo.projects.metio.wtf/tags/options" term="options" label="options"/></entry><entry><title type="html">Release</title><link href="https://ilo.projects.metio.wtf/contributors/release/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/contributors/release/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>The release process of <code>ilo</code> is highly automated, therefore you only have to:</p>
<ol>
<li>Push changes into the <code>main</code> branch.</li>
<li>Wait until an <a href="https://github.com/metio/ilo/blob/main/.github/workflows/release.yml">GitHub action</a> will perform a release automatically. Take a look at the <a href="https://metio.groups.io/g/ilo/calendar">calendar</a> for the next scheduled release.</li>
</ol>
<p>Each new release will:</p>
<ul>
<li>Publish a new GitHub <a href="https://github.com/metio/ilo/releases">release</a></li>
<li>Publish an <a href="https://metio.groups.io/g/ilo/topics">email</a></li>
<li>Publish a <a href="https://fosstodon.org/@metiobuild">toot</a></li>
<li>Publish a post to <a href="https://www.reddit.com/r/metiowtf/">reddit</a></li>
</ul>
]]></content></entry><entry><title type="html">Run Commands</title><link href="https://ilo.projects.metio.wtf/usage/run-commands/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/usage/run-commands/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In order to simplify/automate its usage, <code>ilo</code> will look for <a href="https://en.wikipedia.org/wiki/Run_commands">run command</a> files in the following locations:</p>
<ol>
<li><code>.ilo/ilo.rc</code></li>
<li><code>.ilo.rc</code></li>
</ol>
<p><strong>Each</strong> file found will be added in-order as an <a href="../argument-files">argument file</a> to your invocation of <code>ilo</code> <strong>before</strong> any other options you specify in your terminal.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/usage" term="usage" label="usage"/><category scheme="https://ilo.projects.metio.wtf/tags/run-commands" term="run-commands" label="run commands"/></entry><entry><title type="html">Runtimes</title><link href="https://ilo.projects.metio.wtf/compose/runtimes/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/shell/runtimes/?utm_source=atom_feed" rel="related" type="text/html" title="Runtimes"/><id>https://ilo.projects.metio.wtf/compose/runtimes/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p><code>ilo compose</code> by default searches your local system for supported runtimes. It prefers <code>docker-compose</code> over <code>podman-compose</code> over <code>pods-compose</code> over <code>footloose</code> over <code>vagrant</code> at the moment. In order to force the usage of a specific runtime, use the <code>--runtime</code> flag.</p>
<h2 id="docker-compose">docker-compose</h2>
<p>Force <code>ilo</code> to use <a href="https://docs.docker.com/compose/">docker-compose</a> like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo shell --runtime docker-compose

<span class="c1"># alias</span>
$ ilo shell --runtime dc
</code></pre></div><h2 id="podman-compose">podman-compose</h2>
<p>Force <code>ilo</code> to use <a href="https://github.com/containers/podman-compose">podman-compose</a> like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo shell --runtime podman-copose

<span class="c1"># alias</span>
$ ilo shell --runtime pc
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/compose" term="compose" label="compose"/><category scheme="https://ilo.projects.metio.wtf/tags/runtime" term="runtime" label="runtime"/><category scheme="https://ilo.projects.metio.wtf/tags/docker-compose" term="docker-compose" label="docker-compose"/><category scheme="https://ilo.projects.metio.wtf/tags/footloose" term="footloose" label="footloose"/><category scheme="https://ilo.projects.metio.wtf/tags/podman-compose" term="podman-compose" label="podman-compose"/><category scheme="https://ilo.projects.metio.wtf/tags/pods-compose" term="pods-compose" label="pods-compose"/><category scheme="https://ilo.projects.metio.wtf/tags/vagrant" term="vagrant" label="vagrant"/></entry><entry><title type="html">Runtimes</title><link href="https://ilo.projects.metio.wtf/shell/runtimes/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/compose/runtimes/?utm_source=atom_feed" rel="related" type="text/html" title="Runtimes"/><id>https://ilo.projects.metio.wtf/shell/runtimes/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p><code>ilo shell</code> by default searches your local system for supported runtimes. It prefers <code>podman</code> over <code>docker</code> over <code>lxd</code> at the moment. In order to force the usage of a specific runtime, use the <code>--runtime</code> flag.</p>
<h2 id="docker">Docker</h2>
<p>Force <code>ilo</code> to use <a href="https://www.docker.com/">docker</a>  like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo shell --runtime docker

<span class="c1"># alias</span>
$ ilo shell --runtime d
</code></pre></div><p><strong>Important</strong>: In case your image uses <code>root</code> as its user, and you cannot run <a href="https://docs.docker.com/engine/security/rootless/">rootless docker</a> (introduced with 19.03), use the <a href="../options"><code>--run-as</code></a> option to override the user in the image.</p>
<h2 id="podman">Podman</h2>
<p>Force <code>ilo</code> to use <a href="https://podman.io/">podman</a> like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo shell --runtime podman

<span class="c1"># alias</span>
$ ilo shell --runtime p
</code></pre></div><p><strong>Important</strong>: In case your images uses <code>root</code> as its user, and you cannot run <a href="https://github.com/containers/podman/blob/master/rootless.md">rootless podman</a>, use the <a href="../options"><code>--run-as</code></a> option to override the user in the image. Take a look at this <a href="https://www.redhat.com/sysadmin/behind-scenes-podman">article</a> for an in-depth example of how rootless work with podman.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/shell" term="shell" label="shell"/><category scheme="https://ilo.projects.metio.wtf/tags/runtime" term="runtime" label="runtime"/><category scheme="https://ilo.projects.metio.wtf/tags/docker" term="docker" label="docker"/><category scheme="https://ilo.projects.metio.wtf/tags/podman" term="podman" label="podman"/><category scheme="https://ilo.projects.metio.wtf/tags/lxd" term="lxd" label="lxd"/></entry><entry><title type="html">Security</title><link href="https://ilo.projects.metio.wtf/community/security/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://ilo.projects.metio.wtf/contributors/first-timer/?utm_source=atom_feed" rel="related" type="text/html" title="First Timer"/><link href="https://ilo.projects.metio.wtf/community/help/?utm_source=atom_feed" rel="related" type="text/html" title="Help"/><link href="https://ilo.projects.metio.wtf/usage/help/?utm_source=atom_feed" rel="related" type="text/html" title="Help"/><id>https://ilo.projects.metio.wtf/community/security/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<h2 id="supported-versions">Supported Versions</h2>
<p><code>ilo</code> is using a continuous delivery mode and therefore only supports the latest released version.</p>
<h2 id="reporting-a-vulnerability">Reporting a Vulnerability</h2>
<p>Email <a href="mailto:security@metio.wtf">security@metio.wtf</a> with details about the vulnerability.</p>
<p>Please include some way to reproduce your problem. We are going to provide a fix as soon as possible and include it in the next release.</p>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/community" term="community" label="Community"/><category scheme="https://ilo.projects.metio.wtf/tags/help" term="help" label="help"/></entry><entry><title type="html">Shells</title><link href="https://ilo.projects.metio.wtf/integration/shells/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/integration/shells/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In order to get the most out of <code>ilo</code> make sure it is well integrated into your shell.</p>
<h2 id="bash">bash</h2>
<p>In order to integrate autocompletion into <a href="https://www.gnu.org/software/bash/">bash</a>, follow these steps:</p>
<ol>
<li>Create or edit <code>.bashrc</code>.</li>
<li>Add the following line
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="nb">source</span> &lt;<span class="o">(</span>ilo generate-completion<span class="o">)</span>
</code></pre></div></li>
<li>Reload your shell (or create a new one)</li>
</ol>
<h2 id="zsh">zsh</h2>
<p>In order to integrate autocompletion into <a href="https://www.zsh.org/">zsh</a>, follow these steps:</p>
<ol>
<li>Create or edit <code>.zshrc</code>.</li>
<li>Add the following line
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="nb">source</span> &lt;<span class="o">(</span>ilo generate-completion<span class="o">)</span>
</code></pre></div></li>
<li>Reload your shell (or create a new one)</li>
</ol>
<h2 id="oh-my-bash">oh-my-bash</h2>
<p>In order to integrate autocompletion into <a href="https://github.com/ohmybash/oh-my-bash">oh my bash</a>, follow these steps:</p>
<ol>
<li>Create a new file in <code>~/.oh-my-bash/custom</code>, e.g. <code>~/.oh-my-bash/custom/ilo.sh</code>.</li>
<li>Use the following template
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="nb">source</span> &lt;<span class="o">(</span>ilo generate-completion<span class="o">)</span>
</code></pre></div></li>
<li>Reload your shell (or create a new one)</li>
</ol>
<h2 id="oh-my-zsh">oh-my-zsh</h2>
<p>In order to integrate the autocomplete into <a href="https://github.com/ohmyzsh/ohmyzsh">oh my zsh</a>, follow these steps:</p>
<ol>
<li>Create a new file in <code>$ZSH_CUSTOM</code>, e.g. <code>$ZSH_CUSTOM/ilo.sh</code>.</li>
<li>Use the following template
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="nb">source</span> &lt;<span class="o">(</span>ilo generate-completion<span class="o">)</span>
</code></pre></div></li>
<li>Reload your shell (or create a new one)</li>
</ol>
]]></content><category scheme="https://ilo.projects.metio.wtf/categories/integration" term="integration" label="integration"/><category scheme="https://ilo.projects.metio.wtf/tags/bash" term="bash" label="bash"/><category scheme="https://ilo.projects.metio.wtf/tags/zsh" term="zsh" label="zsh"/></entry><entry><title type="html">tmux</title><link href="https://ilo.projects.metio.wtf/integration/tmux/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/integration/tmux/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p><a href="https://github.com/tmux/tmux">tmux</a> users can simplify the usage of <code>ilo</code> with a tmux session manager. All of them allow you to configure reusable sessions that open project directories and execute commands for you. Consider combining them with <a href="../../usage/run-commands">run commands</a> and/or <a href="../../usage/argument-files">argument files</a> to simplify this even further.</p>
<h2 id="tmuxp">tmuxp</h2>
<p>In order to use <a href="https://github.com/tmux-python/tmuxp">tmuxp</a>, first <a href="https://github.com/tmux-python/tmuxp#installation">install it</a> and then create a new configuration in <code>$XDG_CONFIG_HOME/tmuxp</code>:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat <span class="nv">$XDG_CONFIG_HOME</span>/tmuxp/your-project.yml
session_name: your-project
windows:
- window_name: dev window
  shell_command_before:
    - <span class="nb">cd</span> ~/path/to/your/project
  panes:
    - shell_command:
        - ilo @build-env
</code></pre></div><h2 id="teamocil">teamocil</h2>
<p>In order to use <a href="https://github.com/remi/teamocil">teamocil</a>, first <a href="https://github.com/remi/teamocil#installation">install it</a> and then create a new configuration in <code>~/.teamocil</code>:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat ~/.teamocil/your-project.yml
windows:
  - name: your-project
    root: ~/path/to/your/project
    panes:
      - ilo @build-env
</code></pre></div><h2 id="tmuxifier">tmuxifier</h2>
<p>In order to use <a href="https://github.com/jimeh/tmuxifier">tmuxifier</a>, first <a href="https://github.com/jimeh/tmuxifier#installation">install it</a> and then create a new configuration in <code>$TMUXIFIER_LAYOUT_PATH</code>:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat <span class="nv">$TMUXIFIER_LAYOUT_PATH</span>/your-project.sh
window_root <span class="s2">&#34;~/path/to/your/project&#34;</span>
new_window <span class="s2">&#34;Your Project&#34;</span>
split_v <span class="m">20</span>
run_cmd <span class="s2">&#34;ilo @build-env&#34;</span>
split_h <span class="m">60</span>
select_pane <span class="m">0</span>
</code></pre></div><h2 id="tmuxinator">tmuxinator</h2>
<p>In order to use <a href="https://github.com/tmuxinator/tmuxinator">tmuxinator</a>, first <a href="https://github.com/tmuxinator/tmuxinator#installation">install it</a> and then create a new configuration in <code>~/.config/tmuxinator</code>:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat ~/.config/tmuxinator/your-project.yml
name: your-project
root: ~/path/to/your/project
windows:
  - your_project:
      panes:
        - ilo @build-env
</code></pre></div><h2 id="dmux">dmux</h2>
<p>In order to use <a href="https://github.com/zdcthomas/dmux">dmux</a>, first <a href="https://github.com/zdcthomas/dmux#installation">install it</a> and then create your configuration in <code>$XDG_CONFIG_HOME/dmux/dmux.conf.{file_type}</code>:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell"><span class="o">[</span>you@hostname project-dir<span class="o">]</span>$ cat <span class="nv">$XDG_CONFIG_HOME</span>/dmux/dmux.conf.toml
<span class="o">[</span>your-project<span class="o">]</span>
<span class="nv">number_of_panes</span> <span class="o">=</span> <span class="m">2</span>
<span class="nv">session_name</span> <span class="o">=</span> <span class="s2">&#34;Your Project&#34;</span>
<span class="nv">commands</span> <span class="o">=</span> <span class="o">[</span><span class="s2">&#34;ilo @build-env&#34;</span><span class="o">]</span>
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/integration" term="integration" label="integration"/><category scheme="https://ilo.projects.metio.wtf/tags/tmux" term="tmux" label="tmux"/></entry><entry><title type="html">Version</title><link href="https://ilo.projects.metio.wtf/usage/version/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://ilo.projects.metio.wtf/usage/version/</id><published>2020-04-13T00:00:00+00:00</published><updated>2020-04-13T00:00:00+00:00</updated><content type="html"><![CDATA[<p>In order to see the version of <code>ilo</code> itself, run <code>ilo --version</code> or <code>ilo -V</code>. Its output looks something like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-shell" data-lang="shell">$ ilo --version
ilo: 0.0.0-SNAPSHOT
JVM: 11.0.8 <span class="o">(</span>Oracle Corporation Substrate VM GraalVM 20.2.0 Java 11<span class="o">)</span>
OS:  Linux 5.7.17-200.fc32.x86_64 amd64
</code></pre></div>]]></content><category scheme="https://ilo.projects.metio.wtf/categories/usage" term="usage" label="usage"/><category scheme="https://ilo.projects.metio.wtf/tags/version" term="version" label="version"/></entry></feed>