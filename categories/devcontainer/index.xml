<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Devcontainer on ilo</title><link>https://ilo.projects.metio.wtf/categories/devcontainer/</link><description>Recent content in Devcontainer on ilo</description><generator>Hugo</generator><language>en</language><lastBuildDate>Mon, 13 Apr 2020 00:00:00 +0000</lastBuildDate><atom:link href="https://ilo.projects.metio.wtf/categories/devcontainer/index.xml" rel="self" type="application/rss+xml"/><item><title>Options</title><link>https://ilo.projects.metio.wtf/devcontainer/options/</link><pubDate>Mon, 13 Apr 2020 00:00:00 +0000</pubDate><guid>https://ilo.projects.metio.wtf/devcontainer/options/</guid><description>&lt;p&gt;The &lt;code&gt;ilo devcontainer&lt;/code&gt; command reads a &lt;code&gt;devcontainer.json&lt;/code&gt; (it looks for &lt;code&gt;.devcontainer/devcontainer.json&lt;/code&gt; and &lt;code&gt;.devcontainer.json&lt;/code&gt;, in that order) and opens a shell in the environment it describes. It can be configured with the following command line options. From your terminal, use &lt;code&gt;ilo devcontainer --help&lt;/code&gt; to get a list of all options, and their default values.&lt;/p&gt;
&lt;h2 id="how-a-session-works"&gt;How a session works&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;ilo devcontainer&lt;/code&gt; keeps your container between runs, just like &lt;a href="../../shell/options#how-a-session-is-reused"&gt;&lt;code&gt;ilo shell&lt;/code&gt;&lt;/a&gt;:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;First run:&lt;/strong&gt; &lt;code&gt;ilo&lt;/code&gt; builds (or pulls) the image described by &lt;code&gt;image&lt;/code&gt;/&lt;code&gt;build&lt;/code&gt;, &lt;strong&gt;creates&lt;/strong&gt; a long-lived container, and runs the creation lifecycle commands once.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Later runs:&lt;/strong&gt; &lt;code&gt;ilo&lt;/code&gt; &lt;strong&gt;starts&lt;/strong&gt; the same container again and attaches to it. The creation lifecycle commands are &lt;em&gt;not&lt;/em&gt; run again — only the start- and attach-time ones.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;On exit:&lt;/strong&gt; the container is &lt;strong&gt;stopped but kept&lt;/strong&gt;, so the next run resumes it.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Editing your &lt;code&gt;devcontainer.json&lt;/code&gt; — the image, build inputs (including a referenced &lt;code&gt;Containerfile&lt;/code&gt;), the run configuration, or a lifecycle command such as &lt;code&gt;postCreateCommand&lt;/code&gt; — makes &lt;code&gt;ilo&lt;/code&gt; build a fresh container automatically and re-run the creation lifecycle, because the file&amp;rsquo;s contents are part of the container&amp;rsquo;s identity. So your edits are picked up on the next run without any extra flag. Use &lt;a href="#--fresh"&gt;&lt;code&gt;--fresh&lt;/code&gt;&lt;/a&gt; when you want that even though nothing changed (for example to re-pull a &lt;code&gt;latest&lt;/code&gt; image or reset a container whose state has drifted), or &lt;a href="#--remove-image"&gt;&lt;code&gt;--remove-image&lt;/code&gt;&lt;/a&gt; to remove the container and image entirely when you exit.&lt;/p&gt;</description></item></channel></rss>